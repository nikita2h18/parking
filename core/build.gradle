plugins {
    id 'java'
    id 'org.springframework.boot' version '2.1.1.RELEASE'
}

sourceCompatibility = 1.8

bootRun {
    systemProperties System.properties
}

ext {
    springVersion = '2.1.1.RELEASE'
}

jar {
    manifest {
        attributes 'Main-Class': 'com.gmail.shabannikitka.parking.core.SpringBootConfig'
    }
}

dependencies {
    compile "org.springframework.boot:spring-boot-starter-web:$springVersion"
    compile "org.springframework.boot:spring-boot-starter-data-jpa:$springVersion"
    compile "org.postgresql:postgresql:42.2.4"
    compile "dom4j:dom4j:1.6.1"
    compile "org.modelmapper:modelmapper:0.7.5"
    compile "org.apache.commons:commons-io:1.3.2"
    compile group: 'javassist', name: 'javassist', version: '3.18.2.GA'

    testCompile "org.springframework.boot:spring-boot-starter-test:$springVersion"
}